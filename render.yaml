services:
  - type: web
    name: alpha360-market-server
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python live_price_server.py
    envVars:
      - key: PORT
        value: 8765
      - key: PYTHON_VERSION
        value: 3.11.9
